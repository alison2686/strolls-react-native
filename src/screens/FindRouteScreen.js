import React from "react";
import { View, StyleSheet, Text } from "react-native";

const FindRouteScreen = () => {
  return <Text style={{ fontSize: 48 }}>Find Route</Text>;
};

const styles = StyleSheet.create({});

export default FindRouteScreen;
